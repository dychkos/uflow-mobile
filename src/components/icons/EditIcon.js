import { Icon } from '@ui-kitten/components';
import React from 'react';

export const EditIcon = (props) => (
  <Icon name="edit-outline" {...props} style={{ width: 20, height: 20 }} fill={'#000'} />
);
